<h1 class="ml-3">My Tasks</h1>
<div class="list ml-2 mr-2 pt-3 pb-3">
<% @tasks.each do |task| %>  
  <h3><%= link_to task.title, task_path(task), class: "link-list ml-3" %></h3>
  <% end %>  
</div>
  
<strong><%= link_to "+ Add a new task", new_task_path%></strong>

